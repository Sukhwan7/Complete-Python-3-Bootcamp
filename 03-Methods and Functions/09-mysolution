
import string

def ispangram(str):
    alphabet = list(string.ascii_lowercase)
    for char in str:
        if char in alphabet: 
            alphabet.remove(char)
    return len(alphabet)==0

def ispangram(str):
    a = list()
    for char in str:
        a.append(char)
    return a
